#include <bits/stdc++.h>
using namespace std;
int solve()
{
    long long num;
    cin>>num;
    long long arraySize = pow(2,num);
    cout<<arraySize-1<<endl;
}
int main()
{  
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    int testCases;
    cin >> testCases;
   
    while (testCases--)
    {
            solve();
    }

    return 0;
}

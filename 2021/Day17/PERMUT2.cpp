#include <bits/stdc++.h>
using namespace std;

int main() {
	while(1)
    {
        int num;
        if(num>=1 && num<=pow(10,5))
        {
            for(int i=0;i<num;i++)
            {
                
            }
        }
        else
            return 0;
    }
	return 0;
}
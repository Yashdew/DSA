#include <bits/stdc++.h>
using namespace std;
int main()
{  
    int testCases;
    cin >> testCases;
   
    while (testCases--)
    {
        long long int nums;
        cin>>nums;

        vcetor<long long int> array;
        for(long long int i=0;i<nums;i++)
        {
            long long int temp;
            cin>>temp;
            array.push_back(temp);
        }

        
    }

    return 0;
}

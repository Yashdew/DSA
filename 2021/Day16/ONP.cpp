#include <bits/stdc++.h>
using namespace std;

int main() {
	int t;
    cin>>t;
    if(t>=1 && t<=100)
    {
        while(t--)
        {
            char expression[400];
            cin>>expression;
            if(strlen(expression)>=1 && strlen(expression)<=400)
            {
                for(int i=0;i<strlen(expression);i++)
                {
                    
                }
            }
        }
    }
	return 0;
}
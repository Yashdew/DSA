#include <bits/stdc++.h> 
using namespace std;

int main() {

    int t,cards;
    vector<int> numred,numblue;
    cin>>t;

    if(t>=1 && t<=100)
    {
        while(t--)
        {
            cin>>cards;
            if(cards>=1 && cards<=1000)
            {
                int red,blue;
                for(int i=0;i<cards;i++)
                {
                    cin>>red;
                    cin>>blue;
                    numred.push_back(red);
                    numblue.push_back(blue);
                    
                }
                for(int i=0;i<cards;i++)
                {
                    if()
                }
            }

        }
    }
    
}